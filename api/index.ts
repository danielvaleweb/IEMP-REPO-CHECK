import app from "../server";

// Este arquivo serve como ponte para o Vercel rodar o servidor Express
// Todas as requisições enviadas para /api ou /backend serão processadas por aqui.
export default app;
